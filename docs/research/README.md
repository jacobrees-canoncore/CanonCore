# Research

Investigation output from `/research` and from the grilling session of 8 August 2026. **This is
not domain documentation.** Vocabulary lives in `CONTEXT.md`; decisions live in `docs/adr/`. If a
file here contradicts an ADR, the ADR wins and the file is out of date.

Read the ADRs first. Come here only for the detail an ADR deliberately left out.

| File | Status | Read it when |
| --- | --- | --- |
| [tracker-and-repository-audit.md](tracker-and-repository-audit.md) | **Live reference** | Acting on any finding of the 12–13 August 2026 audit, or writing a ticket that cites one. Sixteen agents verified ticket claims against primary sources and reviewed every merged PR: the security-grade findings (the `NEON_*` owner credentials, the published password digest, the five-project Resend key), the statutory ones (the Schedule 3 clock, the live s.20A duty [CAN-43 Report form, reports table and an administrator queue](https://linear.app/jacobrees-canoncore/issue/CAN-43) defers), the per-ticket corrections, and the architecture diagnosis of the agent-facing documents. Findings are not decisions; each names its primary source. **§9's first two settled-by-owner bullets are superseded** — the 15 and 16 August decisions reversed both; the document carries the dated note |
| [verification-sweep-16-august.md](verification-sweep-16-august.md) | **Live reference** | Acting on any finding of the 16 August 2026 sweep, or wondering why a ticket or document carries a dated 16 August correction. Twenty-six agents, ~400 claims against primary sources, the day after ADR-0014 landed: what held (the RLS non-negotiables, the register under live audit, the statutory quotes verbatim), the ten serious findings (the broken liveness oracle, ADR-0009's phantom redistribution clause, the dead robots.txt premise, the misstated licence roster, both answers to the `s.55(4)` question held at once), and the seven decisions taken on them in the walkthrough — recorded on [CAN-115 Land the 16 August verification sweep: the decisions, the corrections, and what they touch](https://linear.app/jacobrees-canoncore/issue/CAN-115). Findings and decisions are cross-referenced, never restated |
| [per-worktree-preview-databases.md](per-worktree-preview-databases.md) | **Live reference** | Giving each Orca worktree its own preview database, or wondering why the shared one is not enough. Establishes from primary sources that a child of the schema-only `preview` branch is row-free, spends the *total* branch allowance rather than the five root branches, and inherits the seeded Drizzle journal — and that `orca worktree create`'s setup hook runs before any push, which dissolves the race [ADR-0023](../adr/0023-one-shared-schema-only-preview-branch.md) rejected a per-branch database over. Also the drizzle hazard that prompted **CAN-139 The migration script misdescribes how drizzle decides what to apply, and the error hides a silent skip**: migrations are chosen by a timestamp high-water mark, so on a shared database one ordering skips a migration permanently and silently. **Read its *What could not be established* list** — eight items, including the active-compute cap and whether `expires_at` is Early-Access gated, where Neon's guide and its own OpenAPI specification contradict each other. Decision is [CAN-138 Give every Orca worktree its own preview database, so parallel schema work stops colliding](https://linear.app/jacobrees-canoncore/issue/CAN-138) |
| [reducing-the-infrastructure-bill.md](reducing-the-infrastructure-bill.md) | **Live reference** | Looking at the Neon or Vercel bill, or proposing to change a plan, a poll interval or an idle timeout. **The bill is a duty cycle**: the database compute is awake 63.8% of wall clock (measured live on 21 August 2026) because the monitor's 5-minute poll and Neon's 5-minute idle timer are the same number, and lengthening one interval takes Neon compute from about $12.58 a month to about $1.64. Establishes from Neon's own guide that **a shorter idle timeout is a Scale-plan feature and Launch cannot have one**, so buying Scale for it nets about $2 a month at double the rate; that **caching `/` saves $0.00** because row-level security makes a cached render a render for one identity, and the monitor is about to ask a route whose whole job is to touch Postgres; and that **Neon Free is genuinely offered through the Marketplace, `scope: installation`, so it would take `waveger` with it** and would suspend a public service with no spending notification unless the duty cycle is cut first. Also three Vercel line items nobody chose, live-read from the API: **Observability Plus, on by default one second after the Pro upgrade** and outside Spend Management, Elastic build machines billing by default, and a Resend Marketplace installation ADR-0016 would not have bought. **Read its *What could not be established* list** — nine items, including why the dashboard reads $26.28 against $19.25 of metered compute |
| [external-metadata-sources.md](external-metadata-sources.md) | **Live reference** | Touching TMDB, TheTVDB, MusicBrainz, Open Library, Wikidata, tardis.wiki or Big Finish. Endpoints, field names, coverage, rate limits, and the licence terms for each — including which data may be stored and for how long. **How long is settled per Source in [ADR-0014](../adr/0014-shell-providers-and-per-source-retention.md), and TMDB's six-month limit has no exception**; which source we use is [ADR-0009](../adr/0009-external-source-tmdb.md) |
| [source-licence-risk-and-decoupling.md](source-licence-risk-and-decoupling.md) | **Live reference, with known gaps** | Reconsidering the external source, or looking for one outside film and television. Which clause carries the risk — `§1.D` purge-on-termination, not the six-month cache limit — and which other source has one. The seven sources that impose neither, four of which [external-metadata-sources.md](external-metadata-sources.md) never assessed: TVmaze, the Grand Comics Database, Metron and ISFDB. Measured comics and prose coverage, and the finding that **no lawful machine-readable audio-drama source exists**. Why *Ryanair v PR Aviation* means "it is only facts" is not a defence. What CC BY-SA share-alike does and does not reach. The adult-flag survey [ADR-0012](../adr/0012-adult-works-catalogued-artwork-never-displayed.md) depends on: **TMDB's `adult` boolean is the only pornography-specific flag in any source surveyed**, and the comics and prose sources carry age-suitability labels that are a different predicate. And the blast radius of a source change, measured against the working tree. **Read its *Not covered* list** — the TheTVDB image-rights discrepancy is unresolved. Findings, not decisions: the source decision remains [ADR-0009](../adr/0009-external-source-tmdb.md) |
| [transactional-email-providers.md](transactional-email-providers.md) | **Live reference** | Touching email — [CAN-31 Email verification and password reset](https://linear.app/jacobrees-canoncore/issue/CAN-31), or reconsidering the provider. Resend against Postmark, AWS SES, Mailgun, ZeptoMail, Brevo, MailerSend and SendGrid: free tiers, approval gates, the exact DNS records each demands, terms and sub-processors, and what Google and Yahoo actually require of a low-volume transactional sender. The decision is [ADR-0011](../adr/0011-transactional-email-resend.md) and what was provisioned is [`docs/infrastructure.md`](../infrastructure.md); this is the evidence under both |
| [online-safety-act-obligations.md](online-safety-act-obligations.md) | **Live reference** | Writing or reviewing the Online Safety Act documents — [CAN-21 Write the Online Safety Act documents and establish the reporting address](https://linear.app/jacobrees-canoncore/issue/CAN-21) and [CAN-32 Roles, takedown, and the Online Safety Act surfaces](https://linear.app/jacobrees-canoncore/issue/CAN-32). Why the service is in scope and what would take it out; the nine artefacts the duties actually require; the 18 kinds of priority illegal content and what can and cannot be called negligible; the 14 Code measures that bind a smaller low-risk service; the terms-of-service drafting checklist; and which free tools are worth using. **Re-check before launch** — the Act was amended on 29 June 2026 and Ofcom's own toolkit is behind it |
| [production-readiness-baseline.md](production-readiness-baseline.md) | **Live reference** | Deciding what v1 carries beyond features, or planning anything after it. What a production-grade Next.js project holds that [CAN-17 v1: the walking skeleton in production, then the founding case](https://linear.app/jacobrees-canoncore/issue/CAN-17) does not: error tracking, security headers and CSP, rate limiting, backups, SEO surfaces, accessibility and performance gates. Carries the Vercel Hobby and Neon free-plan limits that decide sequencing, the ICO position under [ADR-0020](../adr/0020-no-cookie-consent-banner.md), the free-tier evidence under [ADR-0018](../adr/0018-observability-sentry-and-an-uptime-monitor-outside-it.md), an assessment of `ixartz/Next-js-Boilerplate`, and a list of what was **rejected** and why. **Two of its conclusions are now decisions and this file is the evidence under them, not the record of them** |
| [frontend-design-scope.md](frontend-design-scope.md) | **Live reference** | Designing anything a stranger sees, or scoping the design work. Why the deferred front-end design is three tickets rather than two — [CAN-89](https://linear.app/jacobrees-canoncore/issue/CAN-89) **Give the product a visual identity and a reading surface**, [CAN-90](https://linear.app/jacobrees-canoncore/issue/CAN-90) **Decide how an Ordering reads, and what the interface calls its parts** and [CAN-91](https://linear.app/jacobrees-canoncore/issue/CAN-91) **Check the accessibility conformance neither planned gate can reach** — and why `packages/ui`, Storybook and a design-tokens ticket are not among them. Carries what a Next.js App Router app does and does not share with a later Expo app, the WCAG 2.2 criteria neither planned gate can see, and the typographic numbers that actually bind against the folklore that does not. The styling decision itself is [CAN-50](https://linear.app/jacobrees-canoncore/issue/CAN-50) **Record the styling decision, which CAN-22 made by default** and its ADR |
| [agentic-workflow-setup.md](agentic-workflow-setup.md) | **Live reference** | Changing the Claude Code setup, or deciding how this repository is presented to anyone outside it. Audits `CLAUDE.md`, `.claude/`, the skills and the plugins against what Anthropic actually documents: seven ranked findings, whether wrapping another author's skill is sound, and the features that do not earn their place here. Then the repository as a portfolio piece — what a visitor finds and in what order, what the published evidence says about AI-assisted development and about AI in hiring, and whether the README should say this is agent-built. Defers to [production-readiness-baseline.md](production-readiness-baseline.md) on anything the *product* needs |
| [orca-gaps-and-the-worktree-workflow.md](orca-gaps-and-the-worktree-workflow.md) | **Live reference** | Changing how a lane is created, closed out or documented, or reaching for an Orca surface this repository does not use. Read against Orca **1.4.184**, its shipped source rather than its marketing: the complete `orca.yaml` schema and the source-policy rule that makes a committed file authoritative with no settings change; every Orca surface with a verdict, including the ones rejected and why. **The commissioning ticket's three premises about the setup are each corrected here** — nothing is stored at the settings path it names, what the CLI reports as settings are defaults rather than choices, and a lane does *not* come up installed: `--setup run` runs nothing, and pnpm's `verifyDepsBeforeRun` default is what quietly repairs it. **And one known negative is now out of date: `stablyai/orca#2927` is fixed — removal preserves the branch and reports it**, which is what [CAN-128](https://linear.app/jacobrees-canoncore/issue/CAN-128) **Close out the worktree when /review-pr lands a merge** rested on. Answers whether worktree-off-`main` is the default (it already is, and one documented command is broken) and whether a lane may branch off an unmerged parent (no — squash-merge makes stacking cost a rebase per parent revision). Recommendations, not decisions; the candidate tickets are named and deliberately unfiled |
| [choosing-what-to-dispatch-next.md](choosing-what-to-dispatch-next.md) | **Live reference** | Deciding which tickets to dispatch as lanes, how many may run at once, or what to do when the board is full. The procedure it produced is [`/next-lanes`](../../.claude/skills/next-lanes/SKILL.md). **No published source gives a safe number of concurrent agents** — Orca 1.4.186 states none and says so directly, Anthropic's only number is unmeasured advice for agent teams sharing one checkout, and every vendor cap found is a plan limit — so the ceiling here is policy and is labelled as such. What does predict trouble is **coupling, at an odds ratio of 6.13 against 1.04-1.09 for branch duration** (Dias et al., 73,504 merge scenarios), while Leßenich et al. found *none* of the seven indicators developers believe in has predictive power and concluded indicators must be project-specific. So one was derived here, over a **window pinned to a commit** because the figures went stale within the hour: **59.0% of change-pairs share a file, and documents drive it** — 58.9% against 28.0% for code, with `docs/infrastructure.md` alone in 62% of every merge. Also why the ratio rule behind WSJF cannot be computed here at all (`priority`, `estimate` and `dueDate` are unset on every open issue), why a blocker is scored as a bundle rather than by what it unblocks (Sidney 1975), and what `orca worktree ps` can and cannot be asked. **Read its *What could not be established* list** — ten items, including that no study measures conflict rate against the number of branches open at once, and none covers documentation conflicts at all |
| [document-length-for-agents.md](document-length-for-agents.md) | **Live reference** | Arguing about how long an agent-facing document should be, or shortening one. Measures every always-loaded and pointer-reached document in this repo, then separates the two cost profiles: `CLAUDE.md` has a published 200-line target and was 38% over it when this was measured, since trimmed to 204 loaded lines and at exactly 200 as of 17 August 2026; read-on-demand reference has no published limit and Anthropic's own guidance says to bundle comprehensively. Carries what *Lost in the Middle*, Chroma's *Context Rot*, *Same Task, More Tokens* and *NoLiMa* actually found — including where they do **not** support the popular claim. The restructure it recommends is [CAN-76](https://linear.app/jacobrees-canoncore/issue/CAN-76) **Restructure the agent documents: policy, procedure and incidents get their own homes** |
| [chronology-reference-shape.md](chronology-reference-shape.md) | **Live reference** | Building or importing Orderings. First-hand reading of a real fan chronology: entry counts by medium, the six facet axes, entry types, branching, and the two kinds of unplaced |
| [audiobookshelf-provider-contract.md](audiobookshelf-provider-contract.md) | **Live reference** | Designing or implementing the provider contract. The full prior-art contract, field by field, plus where its schema fails this domain |
| [versions-and-orderings-prior-art.md](versions-and-orderings-prior-art.md) | **Live reference** | Implementing orderings or versions. TMDB Episode Groups and TheTVDB season types are the two public schemas worth borrowing vocabulary from |
| [closing-the-ci-check-gaps.md](closing-the-ci-check-gaps.md) | **Live reference** | Wondering why `scripts/check-docs.ts` reports 11 passed locally and 8 passed / 3 skipped in CI, or proposing to close that gap. Establishes from GitHub's own documentation that the workflow `permissions:` block has exactly sixteen keys and **neither a `secrets` nor an `administration` one**, so no configuration reaches two of the three; that the standard route, a GitHub App via the first-party `actions/create-github-app-token`, mints one-hour tokens from a **private key that never expires**, and so would add a permanent credential to the store the check exists to police; and that no page states an installation token with `Administration: read` receives the `security_and_analysis` block at all. **The verdict is accept all three**, and for the label roster that is design rather than concession: a tracker's taxonomy is not a property of the commit, so a check on it can go red with no push at fault. Its live consequence is the correction now in [`triage-labels.md`](../agents/triage-labels.md) and in [`infrastructure.md`](../infrastructure.md), which both refused a Linear credential partly on a claim Linear's own documentation contradicts |
| [email-testing-inboxes.md](email-testing-inboxes.md) | **Live reference** | Testing anything that sends mail, or verifying a real send without reading a personal mailbox. **Resend's inbound receiving on `mail.canoncore.com` is already provisioned, already a catch-all and already works** — the round trip was measured at 2.4 seconds with `spf`, `dkim` and `dmarc` all passing — so no third-party test-inbox vendor is needed; testmail.app and Mailosaur are priced only as the alternative if a separate receiver is wanted. **Two costs decide the shape**: a round trip spends two of the hundred a day, and reading inbound needs a `full_access` key, which can also delete the sending domain and mint further keys, so this deliberately does not go in CI. The obstacle to using it from a preview was our own guard in [`send.ts`](../../apps/web/src/mail/send.ts), and **CAN-140 Verify a real send against our own inbox, not a personal mailbox** widened it to admit `@mail.canoncore.com` too, minted the `full_access` key and added the by-hand spec this recommends. **No test inbox answers deliverability** — inbox providers publish nothing about filtering — which is why arrival and placement are separate claims |
| [tardis-wiki-extraction.md](tardis-wiki-extraction.md) | **Belongs elsewhere** | Building the provider that reads that wiki. **Move this to the provider repository when it exists** — it is not this product's concern |
| [platform-reach.md](platform-reach.md) | Trimmed | Building the mobile or TV app. Kept: react-native-tvos versioning, focus management, the monorepo TV tax, and the Apple bill. Decision is in [ADR-0005](../adr/0005-stack.md) |
| [work-expression-models.md](work-expression-models.md) | Trimmed | Comparing our model against the standards, or hitting one of the three cases no standard handles. Decisions are in [ADR-0001](../adr/0001-two-levels-story-and-version.md) and [ADR-0002](../adr/0002-orderings-are-separate-from-containment.md) |
| [edits-refresh-and-progress.md](edits-refresh-and-progress.md) | Trimmed | Implementing Progress. Kept the Readium `Locator` reference. The merge survey is spent — its decision is [ADR-0004](../adr/0004-layered-overlay-for-sources-and-edits.md) |

## What was cut, and why

Three files were surveys run to reach a decision. Once the decision became an ADR, the survey was
evidence rather than reference, so each was trimmed to the part still worth consulting: 4,551 lines
down to 504. The ADRs quote the load-bearing evidence directly, so nothing that decided anything
was lost.

Nothing here was written by reading any earlier attempt at this product. Every agent was given that
constraint explicitly and all reported no such source appeared.
